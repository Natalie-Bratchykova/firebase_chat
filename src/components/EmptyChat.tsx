import { Grid } from "@mui/material";

function EmptyChat() {
  return (
    <Grid container justifyContent={"center"} alignItems={"center"}>
      <h1>There is no massages here...</h1>
    </Grid>
  );
}

export default EmptyChat;
